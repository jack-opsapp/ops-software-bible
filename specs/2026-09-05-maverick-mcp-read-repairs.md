# Maverick MCP read and preparation-boundary repairs

Status: the original repairs are live and their authenticated read canaries pass. Jackson also approved the additional timestamp/no-change repair. Its production migration and web deployment are live and independently verified. A fresh signed-in canary passed on 2026-09-06. Real changed-update approval/commit acceptance remains unclaimed.

## Incident and corrected behavior

Authenticated Maverick use-case testing made 40 calls: 29 successes, eight input-validation rejections and three runtime errors. No business change or approval package was created. The unchanged-title preparation probe failed before its no-change guard. These are call counts, not 40 independent scenarios.

- **Grant ordering:** the trusted principal constructor used locale sorting, reversing `ops.catalog.read` and `ops.catalog_costs.read` relative to the stored ASCII catalogue. It now canonicalizes validated ASCII scopes with default string sorting. Exact scope sets, actor/company/client/grant identities, revisions and v14/v9 accepted labels remain mandatory. The regression uses all 21 scopes through the actual principal → actor → service → repository path.
- **Conversation reads:** the provider-source substitution omitted `source_connection_id`, `subject`, `provider_delivery_source_sha256` and `original_content_hash` aliases. The corrective migration restores every required alias for recent turns and cited evidence. Authoritative provider tuples/content, redaction, mailbox/record access and tenant predicates remain intact. Once this error was removed, the live compatibility chain exposed another error: applying manifest-envelope reproof to a raw conversation snapshot. Only conversation wrappers stop that incompatible result transformation. Their input/revision validation and the repository's strict snapshot/authorization validation remain in force. The shared generic proof function is unchanged.
- **All-day task dates:** stored task UTC date parts are inclusive civil date labels, consistent with canonical job-summary scheduling. The detail reader now resolves the company's local start midnight and the exclusive midnight after the final included date. List overlap, overdue, actionable filtering and attention timestamps use the same helper. Each midnight is resolved independently across DST. Timed instants remain unchanged. Paving's stored March 15–16 dates remain untouched and its task-detail end becomes March 17 at 07:00Z.
- **Inputs:** task/site-visit status and section selections and integration selections accept unique values in any order. Read-window timestamps accept UTC seconds with optional 1–3 fractional digits and normalize to milliseconds. Evidence/output timestamp schemas remain strict. Advertised input fields explain UTC, all-day exclusive ends, project-only schedule/readiness, financial components, contact purpose and job kinds. Duplicate/unknown values and substantive authority/conditional restrictions still reject. Immutable catalogue/authorization fingerprints do not change.

## Migration and security boundary

OPS-Web source: `supabase/migrations/20260905184652_agent_maverick_read_repairs.sql`, generated with Supabase CLI 2.116.0. SHA-256 `4c4022d54ba0278ca4d0d33705ce10c358242a00d4dfdb3bbb3a54aace40838d`.

Seven read function definitions change, and `private.agent_task_read_instant(timestamptz,boolean,text,boolean)` is added. The helper has no application-role execute grant. Exact before/after definition hashes reject unexpected source drift and permit replay. Existing function identities, owners, ACLs, security attributes and search paths are preserved. There are no business-row, grant, consent, exposure, table or RLS-policy edits. The original migrations remain immutable. Production ledger: `20260905191357_agent_maverick_read_repairs`. The [archive](../migrations/20260905191357_agent_maverick_read_repairs.sql) is byte-identical to the approved source and ledger statement (16,199 bytes; MD5 `4107d02fa009cb9d992dbee953586e96`).

Company-timezone changes already invalidate the `legacy_operational` revision carried by task proofs, through the existing companies operational-revision trigger. This repair preserves that revision mechanism.

## Verification and live state

- 892 application tests across 78 files pass; full TypeScript checking and focused ESLint pass.
- 60 local PostgreSQL assertions pass: 16 conversation, 41 task and three invariants, plus two reproduced pre-repair errors, safe replay and rejected source drift. Empty/populated conversations, provider-source mismatch, invalid normalization, cited evidence, redaction, tenant/inactive-actor denial, 23/25-hour and multi-day DST dates, discovery/overdue boundaries, current attention, partial dates and timed instants are covered.
- Local SQL readback proves every synthetic business/grant/revision row is unchanged, along with original function identity/owner/ACL/security settings. The runner is `bash tests/sql/agent-maverick-repair-run-runtime.sh` and owns only its disposable Unix-socket database.
- Production deployment `dpl_8sC7eyrxAyJSEZ7wF5Kwk2x7J8x6` is READY on exact OPS-Web commit `2a349d8182e449af8891332f5b9636f3121df652` and owns `app.opsapp.co`. It includes grant-order commit `41374fc87` and the read repairs.
- Post-apply SQL readback verified all seven target hashes, preserved function identities/owners/ACLs/security/search paths, denied application execution on the helper, and matched the exact ledger statement.
- Authenticated canaries after deployment: task detail and full project summary agree on Paving's exclusive March 17 07:00Z end; list overlap includes its final civil day and excludes it after that instant. Both list responses are terminal pages. Unsorted task/integration/site-visit selections, seconds-only site-visit windows, customer job kinds and opportunity financial components succeed.
- Conversation returns the expected non-retryable `NOT_FOUND`. Independent inventory found zero Maverick conversations, turns and anchors; this proves the empty live case. Populated and security cases remain locally verified.
- Independent final readback at 2026-09-05 23:32:17 UTC retained the exact Paving row and active 21-scope grant, with zero Maverick customer-update proposals. A subsequent full opportunity-row digest matches the pre-probe digest `9cb4042b9a24099d8588ed0f8aec633b`; its exact source timestamp is unchanged.
- GitHub CI run [33986578832](https://github.com/jack-opsapp/ops-web/actions/runs/33986578832) fails at the unchanged delivery-source fixture's `agent_provider_delivery_source_idempotency_conflict`. This pre-existing failure is separate from the successful deployment and targeted repair checks.

The local fixture uses frozen live definitions/column types and real authorization helpers; unrelated FKs, write-trigger graphs and provider workers are not reproduced. Local job-summary agreement uses the established helper/formula; authenticated post-release calls additionally proved full task-detail/project-summary agreement. Original live reports remain preserved under OPS-Web `docs/artifacts/phase12/`; repair proof and the readable SQL delta are under `docs/artifacts/maverick-repair/`.

## Separate timezone platform finding

Production resolves Vancouver midnight on November 2, 2026 as 08:00Z; current local PostgreSQL/Node resolve 07:00Z. B.C. [adopted permanent UTC−7 after March 8, 2026](https://news.gov.bc.ca/releases/2026AG0013-000209). Production's timezone data therefore remains older than the rule used by current application runtimes. This repair follows the existing database civil-date resolver and does not claim to solve that platform mismatch. A production timezone-data refresh and subsequent cross-runtime verification remain unresolved and require operational assessment. DST fall-back fixtures use Los Angeles, where the transition still exists.

## Additional defects exposed by the live no-change canary

The first diagnostic submission used SQL timestamp syntax and was correctly rejected by the ISO input schema before RPC. The corrected submission copied the actual `get_job_summary` identity timestamp with milliseconds and reached `AGENT_CUSTOMER_UPDATE_SOURCE_STALE`. The stored timestamp contains six fractional digits, so the read had discarded information required by exact optimistic concurrency. Supplying the exact six-digit version reached `AGENT_CUSTOMER_UPDATE_NO_CHANGE`, proving the grant-order repair; the application incorrectly projected that guard as retryable `TEMPORARILY_UNAVAILABLE`. Neither request created a proposal or business change.

Approved source commit: `bed132976`. Production integration commit `e7c530015d9e4715626e4c32be3b0e82177d6cb6` preserves the changes already on remote main and passed the same 178 tests. Approved source migration: `20260905192721_agent_customer_update_source_precision.sql`; SHA-256 `73d4218ee05f74c2230295790e69581ebe8ad1dcb33abbf0defc2b256afe927d`. It changes only the job-summary core's identity `dates.updated_at` expression to UTC with six fractional digits. The shared millisecond formatter and exact mutation-version equality remain unchanged. Original function definition MD5 `128a904a5ddaa81ab387fdddf95210e4` becomes `e8ddba2e98fb82a468aa2ce1e0a792e1`; unexpected definition/security drift blocks application, and OID/owner/ACL are preserved.

The application separately maps only exact `AGENT_CUSTOMER_UPDATE_NO_CHANGE` to non-retryable `INVALID_ARGUMENT`, field issue `CUSTOMER_UPDATE_NO_CHANGE`, with “The requested values already match this record. No proposal was created.” The input description tells callers to copy the exact identity version. No authority, consent, effect policy, stale guard or business trigger is relaxed.

Local proof: the original projection and real preparation RPC reproduce the stale failure; twelve PostgreSQL assertions prove exact precision, valid equivalent offset, a one-microsecond concurrent edit remaining stale, rounded versions still rejected, successful changed synthetic preparation without a business mutation (rolled back), no change-set/action/row effects, preserved function security and unchanged shared formatter. Replay passes and source drift rejects. 178 application tests across four files pass; full TypeScript and focused lint pass. The strengthened nine-test transport suite also passes. Application proof covers the real job-summary repository accepting a proof-bound six-digit identity, service/repository forwarding all digits, and correct no-change transport. The SQL fixture evaluates the installed live projection expression and actual preparation authority/guards; it does not execute the entire summary RPC. Production database verification confirms the expected new definition and unchanged original OID/owner/ACL/security/search path, shared formatter, preparation and authority function hashes, full opportunity/task digests, active grant and zero proposals. Ledger `20260905235844_agent_customer_update_source_precision` matches the approved source byte-for-byte; its [archive](../migrations/20260905235844_agent_customer_update_source_precision.sql) is 1,388 bytes (MD5 `c89defd31a0065dd05e8aba9130a7b63`).

## Approved follow-up release and verification

Jackson explicitly approved the precision migration, web release, Bible publication and final verification records. The earlier automatic review rejection of the documentation push is superseded by that direct approval.

The precision migration was applied as ledger `20260905235844` and independently verified at 2026-09-05 23:59:10 UTC. Web integration commit `e7c530015d9e4715626e4c32be3b0e82177d6cb6` was pushed to `main`; deployment `dpl_GLWfrf9ptukEqCdWySBexDACUQNg` is READY and independently resolves from `app.opsapp.co` at that exact source version. Post-deployment SQL readback at 2026-09-06 00:06:35 UTC retains every expected function/security attribute, unchanged task/opportunity/grant state and zero proposals. The machine-readable [release proof](../docs/artifacts/maverick-mcp-repairs/precision-release.json) records deployment, exact hashes and verification limits.

GitHub run [34000083879](https://github.com/jack-opsapp/ops-web/actions/runs/34000083879) reaches the same existing failure at `tests/sql/delivery-source-reprojection-contract.sql:498`, `agent_provider_delivery_source_idempotency_conflict`. This is unchanged from the first release and separate from the successful targeted checks.

The final fresh identity → unchanged-title MCP canary is now complete. On 2026-09-06 the signed-in OPS connector returned Maverick opportunity `d2000000-0000-4000-d200-000000000008` with exact `updated_at` `2026-08-21T20:19:43.482755Z`. Passing that unchanged title and full timestamp to `prepare_customer_update` returned non-retryable `CUSTOMER_UPDATE_NO_CHANGE`; no proposal was created. Independent database readback confirmed the title and timestamp were unchanged, `private.agent_customer_updates` remained empty for Maverick, and Maverick `job_conversations` remained empty. This proves exact precision and the no-change transport. It does not prove a real changed-update approval or commit.

Creating a real production proposal, approving/committing a business change, and sending messages remain outside this repair release. No new service or paid tier is introduced; existing hosting/database usage applies.
