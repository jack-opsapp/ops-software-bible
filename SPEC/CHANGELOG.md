# SPEC — Spec Revision Changelog

## 2026-07-16 — /spec/confirmation reworked for Tier Model v2 payment shapes

The deposit confirmation surface still carried v1 tier maps (setup/build/enterprise at v1 prices) while every v2 Stripe session carries spec01/02/03 — real confirmations rendered a raw uppercased slug for the package, an em-dash total, and no checkpoint amounts. 04's § /spec/confirmation was also still the v1-generic "4 milestones" spec, which 10 § 5 had retired for Tier 1; the section now specifies the per-tier contract. Rebuilt per 10 § 2/§ 5/§ 6: SPEC-01 renders its real 50/50 three-stop journey (scope sign-off as an evidence-only stop, completed chip SIGNED), SPEC-02 keeps the 4×$1,875 rhythm, SPEC-03 shows the deposit fixed against the floor and the honest "from $25,000 / locks at scope sign-off" total until `spec_projects.locked_total_cents` is set (exact thirds after — the confirmation route now fetches the column). Display names come from the dictionary designations (per 10 § 6), every amount derives from `lib/spec/pricing.ts` via the new unit-tested `lib/spec/confirmation-schedule.ts`, and the previously-orphaned `confirmation.timeline.<tier>` dictionary lines are wired in under the rail. Implementation on ops-site `feat/spec-tier-model-v2` (`da37272`), evidence pack `ops-site docs/artifacts/spec-v2/confirmation-v2-*.png`.

## 2026-07-14 — Tier Model v2 (design approved, implementation pending)

Jackson redefined the tier ladder in a brainstorm session; the approved design is captured in [10_TIER_MODEL_V2.md](10_TIER_MODEL_V2.md). Summary: Setup/Build/Enterprise → **SPEC-01 · WORKFLOWS** ($2,000, 50/50, customer-owned tool wiring, no OPS required), **SPEC-02 · SYSTEMS** ($7,500 + required $395/mo care, outcome-defined data backbone in-OPS or standalone), **SPEC-03 · PROPRIETARY** (from $25,000 locked at scope sign-off + from $750/mo care, a standalone trade tool built only for the customer — Deckset-class, never OPS-scale — with app-instance exclusivity: OPS never sells their app, but code reuse across other builds is unrestricted), plus a **white-label add-on** on SPEC-03 (+$4,000 / +$200/mo, published turnkey under OPS's Apple Developer account, App Transfer as the off-boarding escape). Retired: the subscription-multiplier concept and the universal 25/25/25/25 (01 is 50/50; 03 has a variable total). Carried unchanged: owner-approval-before-Stripe, Quebec exclusion, guarantee policy (per-tier levers now vary), $399 Data Setup add-on, Phase 0 conversation-only posture until the v2 legal prose ships. Pricing grounded in a sourced 2025-26 market sweep (see 10 § header). README status + tier table updated; the full doc-pass across 01-07 lands with implementation.

## 2026-05-25 — Gate resolution pass

The six implementation bug_reports added by the fourth-pass were resolved in a dedicated investigation pass against live infrastructure on 2026-05-25. The pass verified every assumption against the live `ops-app` Supabase project (`ijeekuhbatykdomumfjx`), the live OPS-Web signup/company-provisioning code, the live OPS-Web `dashboard-layout.tsx` + `notifications-drawer.tsx` mount, and the current Stripe Checkout API documentation. After this pass, the spec has zero open implementation gates. All resolution detail lives in [07_ROLLOUT.md](07_ROLLOUT.md) § Gate resolutions; this entry summarizes what changed and where the locked answers live.

### Per-gate summary

**SPEC-STRIPE-ADDRESS-TAX-SPIKE — Resolved.** Stripe Checkout (hosted Session) is the locked payment UI. Session parameters compose: `customer` (pre-fills the OPS Customer's saved address), `customer_email`, `automatic_tax: { enabled: true }` (Stripe Tax derives GST/HST/PST from billing province for Canada), `billing_address_collection: 'required'`, `consent_collection: { terms_of_service: 'required' }` (the dedicated ToS mechanism — `custom_fields` does NOT support checkboxes in hosted Checkout), `phone_number_collection: { enabled: true }`, `custom_fields` for optional GST/HST. The hosted page allows the customer to edit the pre-filled billing address (Stripe has no parameter to lock it), so the webhook gains a mandatory Quebec defense: on `checkout.session.completed`, if `session.customer_details.address.state === 'QC'` (or `country !== 'CA'`), refund the captured payment via Stripe Refunds API, cancel the project (`cancellation_reason='quebec_billing_at_stripe'`), add the buyer to `spec_blocked_buyers` (misrepresentation per ToS § 3), notify, return. The fallback "if Checkout doesn't fit, use Payment Element" language is removed throughout the spec. Stripe Dashboard one-time setup: ToS URL `https://opsapp.co/legal?page=spec-terms`, Stripe Tax enabled for Canada, CAD default currency. Conclusions folded into 07_ROLLOUT.md § Gate resolutions, 01_BUSINESS_MODEL.md § 1 + § 2, 03_WORKFLOW.md § Path A + webhook flow, 04_CUSTOMER_UX.md § billing-address.

**SPEC-LIVE-SCHEMA-MISMATCHES — Resolved.** Live schema verified column-by-column for every SPEC reference. Confirmed clean: `users.id uuid`, `companies.account_holder_id text`, `companies.admin_ids text[]`, `role_permissions(role_id, permission, scope)` all NOT NULL with `scope='all'` canonical (280 of 314 rows), `user_roles.user_id text`, `has_permission(uuid, text, text)` signature, `get_user_id() returns text`, `notifications.user_id text` + `company_id text NOT NULL`, `pg_cron 1.6.4` (in `cron` schema), `citext` not yet installed (idempotent `create extension if not exists` covers it), `companies.slug` confirmed absent, `user_permission_overrides.expires_at` confirmed absent. Two new findings folded in: `user_permission_overrides.company_id` is `uuid NOT NULL` — every SPEC operator override row must carry `company_id = OPS_OPERATIONS_COMPANY_ID` by convention; and `public.roles` requires `name`, `hierarchy int NOT NULL`, `is_preset boolean NOT NULL` columns — the SPEC Operator role seed in `migrations/YYYYMMDD_spec_phase1_operator_gate.sql` is updated to populate all four (`id='00000000-0000-0000-0000-0000000000a1'`, `name='SPEC Operator'`, `hierarchy=0`, `is_preset=true`, `company_id=null`). Confirmed `has_permission()` body short-circuits via `is_company_admin / account_holder_id / admin_ids` before consulting `role_permissions` — validating the SPEC requirement for a separate operator gate. Conclusions folded into 07_ROLLOUT.md § Gate resolutions and 02_DATA_MODEL.md § Operator gate.

**SPEC-SECURITY-DEFINER-PRIVATE-SCHEMA — Resolved.** Both SECURITY DEFINER functions (`is_spec_operator()` and `refresh_spec_board_snapshot()`) placed in the existing `private` schema, matching the OPS convention (existing private functions: `private.current_user_has_permission`, `private.resolve_uid`, `private.current_user_is_admin`, `private.get_user_company_id`, others). Live grant pattern verified: `private` schema USAGE granted to `authenticated` only (NOT to `anon`), function EXECUTE granted to `public` (resolves to "anything with USAGE on private" — i.e., authenticated only). SPEC follows the same pattern: USAGE on `private` granted to `authenticated, service_role`; EXECUTE on `private.is_spec_operator()` granted to `public`; EXECUTE on `private.refresh_spec_board_snapshot()` granted to `service_role` only. pg_cron runs as `postgres` which owns the function — no grant needed for the scheduled run. The pg_cron schedule becomes `select private.refresh_spec_board_snapshot();`. Every RLS policy and every admin route updates from `public.is_spec_operator()` to `private.is_spec_operator()`. Conclusions folded into 07_ROLLOUT.md § Gate resolutions, 02_DATA_MODEL.md § Operator gate + § Public board snapshot + RLS table, and 05_ADMIN_UX.md § Permissions.

**SPEC-NO-COMPANY-BUYER-FLOW-LOCK — Resolved.** A buyer MUST have `users.company_id` populated before they can submit the `/spec/billing-address` form. No-company buyers are redirected to the existing OPS-Web `/setup` flow, then return to `/spec?tier=X` after the company step completes. Proof: live OPS-Web flow inspection — `ops-web/src/app/api/auth/sync-user/route.ts` creates `users` rows with `company_id=null` on Firebase sign-in; `register/page.tsx` routes to `/account-type`; `AccountTypeScreen.handleContinue` routes "Run a Crew" choice to `/setup`; `ops-web/src/app/api/setup/progress/route.ts` (lines 103–161) creates the company on `step='company'` with `account_holder_id=userId` + `admin_ids=[userId]`, calls `initialize_company_defaults()`, links the user as `is_company_admin=true`. `resolveSpecCompanyForProject(buyerUserId, tier)` is locked: returns `{ ok: false, redirectTo: '/setup?returnTo=/spec?tier=X' }` for no-company users; returns `{ ok: true, companyId, accountHolderUserId, isBuyerAccountHolder }` otherwise. The `isBuyerAccountHolder` flag drives Path A vs Path B branching. Every `spec_projects.linked_company_id` is guaranteed non-null. The deposit gate runs before address form rendering. Conclusions folded into 07_ROLLOUT.md § Gate resolutions, 01_BUSINESS_MODEL.md § 1, 03_WORKFLOW.md, and 04_CUSTOMER_UX.md § intake form solo-buyer section.

**SPEC-NOTIFICATION-RAIL-DEPRECATED — Resolved as a false alarm.** The notification rail is NOT deprecated — it is alive as an edge-tab drawer (visual placement changed from header to edge-tab; the rail itself is active). Proof: `ops-web/src/components/layouts/dashboard-layout.tsx:15` imports `NotificationsDrawer`, lines 291–292 mount `<NotificationsDrawer />` + `<NotificationsTab />`, `notifications-drawer.tsx:41` calls `useNotifications()` reading `public.notifications`. Live data: 291 rows, max created_at 2026-05-19, 16 distinct types active in last 30 days (`mention`, `task_review_stack`, `system_alert`, `gmail_sync`, `trial_expiry`, `expense_submitted`, `payment_review_stack`, `leads_waiting`, `task_completion`, `pmf_alert`, others). SPEC writes directly to `public.notifications` for all operator and customer events. Routing locked per-event: customer operational notices use email (primary) + in-app rail with `company_id = linked_company_id` (secondary); operator workflow events use the in-app rail with `company_id = OPS_OPERATIONS_COMPANY_ID` + TODAY queue + email for high-urgency subset (Stripe dispute, refund requested, regulated workflow flagged, related-entity flag, 7-day comms cadence missed). All "after live-code verification" hedge language removed. New helpers `dispatchSpecOperatorNotification()` and `dispatchSpecCustomerNotification()` added as Phase 1 implementation tasks. Conclusions folded into 07_ROLLOUT.md § Gate resolutions, 03_WORKFLOW.md, 05_ADMIN_UX.md status-change side effects, and 06_CONTRACT_AND_EMAILS.md § Internal events.

**SPEC-SERVER-ROUTES-VS-RAW-RLS-DECISION — Resolved.** Phase 1 exposes ZERO raw SPEC table reads/writes to anon or authenticated client SDKs, except `spec_public_board_snapshot` (sanitized aggregate public read). Every customer-facing surface goes through a server route that uses the service-role client + explicit `buyer_user_id || account_holder_user_id` authorization check + narrow projection. Complete Phase 1 customer-route enumeration (path, method, auth, input, output, DB writes) lives in 07_ROLLOUT.md § Gate resolutions: `GET /api/spec/board` (public, sanitized aggregate only), `POST /api/spec/create-checkout-session` (authenticated, resolves company), `POST /api/spec/owner-approval/[token]` (authenticated, account_holder gated), `GET /api/spec/checkout/[buyer_checkout_token]` (authenticated, buyer gated), `POST /api/spec/intake/submit` (intake-token gated), `POST /api/account/spec/[id]/request-refund` (authenticated, buyer/account_holder gated, safe-fields-only), `POST /api/admin/spec/board/refresh` (operator gated). Phase 2 customer portal at `/account/spec/[id]` continues the same server-route posture. RLS posture table confirms: anon + authenticated cannot SELECT/INSERT/UPDATE/DELETE on any SPEC table except `spec_public_board_snapshot` SELECT. Migration handoff includes tests proving the posture. Conclusions folded into 07_ROLLOUT.md § Gate resolutions, 02_DATA_MODEL.md § RLS and server-route default, and 04_CUSTOMER_UX.md route references.

### Cross-file consistency after this pass

- Every `public.is_spec_operator()` reference replaced with `private.is_spec_operator()` across 02_DATA_MODEL.md, 05_ADMIN_UX.md, and 07_ROLLOUT.md.
- Every `public.refresh_spec_board_snapshot()` reference replaced with `private.refresh_spec_board_snapshot()`. pg_cron schedule call updated.
- Every "TBD by implementation discovery", "after live-code verification", "gated by SPEC-X", and "must be proven in test mode" hedge phrase removed from the body files; replaced with the locked answer or a cross-reference to 07_ROLLOUT.md § Gate resolutions.
- Notification-routing language is consistent across 03_WORKFLOW.md, 05_ADMIN_UX.md, 06_CONTRACT_AND_EMAILS.md, and 07_ROLLOUT.md: rail is active; customer rows use `linked_company_id`; operator rows use `OPS_OPERATIONS_COMPANY_ID`.
- No-company buyer language is consistent across 01_BUSINESS_MODEL.md, 03_WORKFLOW.md, 04_CUSTOMER_UX.md, and 07_ROLLOUT.md: deposit gate enforces company exists; `/setup` flow handles provisioning; `spec_projects.linked_company_id` is guaranteed non-null.
- Stripe Checkout posture is consistent across 01_BUSINESS_MODEL.md, 03_WORKFLOW.md, 04_CUSTOMER_UX.md, and 07_ROLLOUT.md: hosted Session with `consent_collection.terms_of_service='required'`, `automatic_tax.enabled=true`, `customer` pre-fill, webhook Quebec defense.
- Live-schema verification line in 02_DATA_MODEL.md head expanded to cite every queried column + function + extension.

### Items NOT changed in this pass

- The three tier prices ($3,000 / $8,500 / $18,000) and the 25/25/25/25 split.
- The CAD-only launch.
- The OPS Operations company UUID (`00000000-0000-0000-0000-00000000000a`).
- The dedicated SPEC Operator role UUID (`00000000-0000-0000-0000-0000000000a1`) — assigned for the first time in this pass; reserved going forward.
- The legal launch gates: final customer-facing ToS / Privacy / DPA prose remains required before automated live deposits. Counsel review remains recommended risk mitigation, not a hard blocker per Jackson's owner decision.
- Phase 0 conversation-only launch posture.
- The 30-day Guarantee Refund window + anti-abuse rules.
- The pre-Stripe billing-address rejection (Quebec block) at `/spec/billing-address` — the webhook defense is additive, not a replacement.
- The 18 verification scenarios in 07_ROLLOUT.md (two new scenarios — 19 webhook Quebec defense, 20 no-company buyer gate — added to bring the total to 20).

### Operational follow-ups remaining (not blocked on spec)

- Final customer-facing ToS, Privacy Policy, and DPA prose.
- Stripe Dashboard one-time setup (ToS URL, Stripe Tax for Canada, CAD currency).
- One-line addition to `ops-web/src/app/(onboarding)/setup/page.tsx` for `returnTo` query-param honoring (Phase 1 implementation task).
- Founder media assets (welcome video, hero presence).
- Calendly / Cal.com choice (discovery + walkthrough scheduling).
- Meta CAPI + Google Enhanced credentials procurement (Vercel env).
- Stripe Connect Express setup (Phase 2 referrals).
- Billing-engine wiring for `spec_module_entitlements` multiplier + surcharge consumption.
- A separate hygiene pass on the root `CLAUDE.md` "notification rail in the header" line — the rail is now an edge-tab drawer, not in the TopBar; the documentation should reflect this.

---

## 2026-05-25 — Fourth revision pass (legal/commercial correction pass)

An external legal/pro review found the third-pass SPEC ready for **conversation-only Phase 0**, but not ready for automated live SPEC deposits. This pass keeps the productized SPEC model intact and corrects the legal/commercial launch gates and implementation-risk tracking.

### Summary

- Phase 0 conversation-only launch is approved. Automated live SPEC deposits remain blocked until final customer-facing ToS / Privacy / DPA text exists and the fourth-pass legal fixes are applied.
- Counsel review is recommended risk mitigation, not a hard launch blocker per Jackson's owner decision. Final legal prose before live deposits is required; a section outline is not enough.
- Limitation of liability, guarantee, and IP/license language replaced across the business model and contract drafting requirements.
- Quebec exclusion tightened to cover billing address, head office, operating address, establishment, and material SPEC use in Quebec. Misrepresentation is a material breach and guarantee-ineligible.
- CASL handling clarified: referrals are link-based, OPS does not cold-email referred prospects, and retainer/promotional emails need sender identity, mailing/contact info, unsubscribe, and a consent basis.
- Refund request route moved to a customer account route: `/account/spec/[id]/request-refund`; `/admin/spec/refunds` remains the operator processing queue.
- Deprecated header-notification assumptions removed or flagged. SPEC now uses email for customer operational notices, the admin TODAY queue for operator work, and the current OPS notification channel only after live-code verification.
- RLS/server-route uncertainty locked as `SPEC-SERVER-ROUTES-VS-RAW-RLS-DECISION`: Phase 1 customer SPEC reads/writes default to server routes returning narrow projections, not raw table access.
- No-company buyer flow locked as `SPEC-NO-COMPANY-BUYER-FLOW-LOCK`: implementation must inspect the current signup/company provisioning flow before freezing `resolveSpecCompanyForProject()`.
- Stripe address/tax uncertainty added as `SPEC-STRIPE-ADDRESS-TAX-SPIKE`: prove the payment UI in Stripe test mode before relying on Checkout behavior.
- Security-definer and live-schema mismatch bug_reports added: `SPEC-SECURITY-DEFINER-PRIVATE-SCHEMA` and `SPEC-LIVE-SCHEMA-MISMATCHES`.

---

## 2026-05-25 — Third revision pass (post-second-external-review)

A second external review of the locked-after-first-revision spec surfaced seven critical implementation-breaking findings, ten major findings, and seven minor findings, plus seven locked answers from a follow-up session with Jackson. This pass rewrites all eight SPEC files to address every one.

### Per-file summary

**README.md** — Table count updated from "14 new tables" to "17 new tables" (16 SPEC engagement tables + 1 public board snapshot table). SPEC admin gate language updated: the canonical SPEC operator check is now `public.is_spec_operator()` — a dedicated SECURITY DEFINER function — NOT `public.has_permission(...)`, which short-circuits through customer-company admin status. Status line updated to "third revision pass complete." Open-items pointer updated to note that the RLS audit and test-environment strategy moved into Phase 1 launch gates.

**01_BUSINESS_MODEL.md** — Buyer-identity flow rewritten with the pre-Stripe billing-address collection step (§1, step 2): the address form runs server-side BEFORE any Stripe Checkout Session is created, and `province == 'QC'` rejection happens there. The Path B flow now explicitly captures the account_holder's binding acceptance as a `spec_acceptance_events` row with `event_type = 'owner_purchase_approved'` (with IP, UA, and the ToS version hash the account_holder reviewed). The buyer's subsequent Stripe checkout writes a separate `tos_accepted` row. The 30-day money-back guarantee text was rewritten to acknowledge net-15 invoicing and describe per-milestone refund mechanics — refund (paid Payment Intent), void (open invoice), credit_note (partially paid), mark_uncollectible (non-cancellable open) — each recorded as a line in `spec_refund_requests.refund_breakdown`. The "Stripe Checkout collected fields" section noted that the address was OPS-canonical and Stripe's address widget was hidden; the fourth pass supersedes that with the Stripe address/tax spike. The prior BC-court-certainty line was softened to "intended legal framing pending counsel review." The edge-cases section's pre-Stripe gate clause was extended to mention pre-Stripe billing-address collection alongside the auth + owner-approval gate.

**02_DATA_MODEL.md** — Largest single rewrite of the pass. Added a `public.is_spec_operator()` SECURITY DEFINER function and refactored every admin RLS policy + admin route to use it. Documented why `has_permission()` is unsafe as the SPEC gate (short-circuits through `is_company_admin`, `account_holder_id`, `admin_ids` before consulting `role_permissions`). Made `spec_projects.tos_version_accepted` and `tos_accepted_at` nullable + added a CHECK constraint that requires them once `status` leaves the pre-deposit states (`awaiting_owner_approval`, `awaiting_deposit`). Replaced the broken `spec_board_counts` view with a `spec_public_board_snapshot` TABLE refreshed every 5 minutes by `pg_cron` calling `public.refresh_spec_board_snapshot()` (SECURITY DEFINER); anon SELECT is granted on the table, edge cached via the `/api/spec/board` route's `Cache-Control: public, max-age=300`. Added a manual force-refresh path for operators. Added `spec_refund_requests.refund_breakdown` jsonb and extended `spec_payment_status` with `voided`, `partially_refunded`, `uncollectible`. Added `owner_purchase_approved` to the `spec_acceptance_events.event_type` CHECK constraint so the account_holder's binding acceptance is captured as a first-class evidence event (not stuffed into `spec_communications`). Added `billing_address_line1` / `line2` / `city` / `province` / `postal_code` / `country` to `spec_projects`, plus the `no_quebec` CHECK constraint. Changed `spec_module_entitlements.enabled` default from `true` to `false` and added `'not_yet_delivered'` to `disabled_reason`. Added a "Supabase Storage configuration" section documenting the `spec-intake` bucket: not public, 25MB per file, MIME-whitelisted, signed URLs 24h TTL, deletion 90 days after engagement terminal-state. Seeded an "OPS Operations" internal company (constant UUID `00000000-0000-0000-0000-00000000000a`) so operator-facing notifications and no-company-buyer notifications have a valid company_id. Replaced plaintext `approval_token` and `buyer_checkout_token` columns with `approval_token_hash` / `buyer_checkout_token_hash` (bcrypt/argon2; URL emits plaintext, DB stores hash). Changed `spec_blocked_buyers.email` to `citext`. Dropped `spec_referrals.ytd_referrer_payout_cents` (derived from `sum() WHERE status='paid' AND paid_at >= date_trunc('year', now())`). Added `is_test boolean not null default false` to every SPEC engagement table. Replaced the long-tail "RLS mirrors spec_scope_documents pattern" placeholder with explicit, CONCRETE per-table policies for all nine tables (spec_payments, spec_change_orders, spec_feature_acceptance, spec_satisfaction_ratings, spec_support_tickets, spec_retainers, spec_communications, spec_refund_requests, spec_referrals). Migration file list expanded from 5 to 8 files, including a dedicated operator-gate migration and a Storage migration.

**03_WORKFLOW.md** — Path A and Path B both gain the pre-Stripe billing-address step (§ Address capture) explicitly described as the canonical Quebec block. Path B's account_holder approval now writes a `spec_acceptance_events` row with `event_type = 'owner_purchase_approved'` (with IP, UA, and ToS hash). The webhook flow for Stripe completion now writes the buyer's `tos_accepted` event AND populates `spec_projects.tos_version_accepted` / `tos_accepted_at` (which were null until that moment — the CHECK constraint permits pre-deposit). Module entitlement reservation at scope sign-off explicitly notes `enabled = false`, `disabled_reason = 'not_yet_delivered'`, with the flip to `enabled = true` happening at delivery walkthrough. A new "Refund processing — per-milestone procedure" section spells out the processor as pseudocode: for each milestone, dispatch to refund / void / credit_note / mark_uncollectible based on current `spec_payments` state; record each action in `refund_breakdown`. The queue-count semantics are locked: queue = `awaiting_deposit` + `deposit_paid` (was inconsistent across files). The state-machine diagram was annotated with the new acceptance events. Token-hash language added throughout for the owner-approval and buyer-checkout tokens.

**04_CUSTOMER_UX.md** — OPS BOARD data path switched from the broken `spec_board_counts` view to the `spec_public_board_snapshot` table, with the `/api/spec/board` route emitting `Cache-Control: public, max-age=300, s-maxage=300, stale-while-revalidate=60`. The `refreshed_at` column is exposed in the payload and is the authoritative freshness signal in the UI (amber after 72h). Added a new `/spec/billing-address` page that captures line1/line2, city, province, postal code, country pre-Stripe; server-side rejects `province == 'QC'` and `country != 'CA'`. Third-pass Stripe Checkout address assumptions are superseded by the fourth-pass Stripe address/tax spike. The intake "Welcome" template's "Hey [name]" warm opener was replaced with the tactical OPS voice: `// INTAKE READY` / `[Tier] build file opened for [company].` The "No goalpost moving" / "Disputes resolved by reading the doc" copy in "Standing Behind The Work" was replaced with neutral acceptance language: "Scope is written before build. Acceptance is measured against that written scope. If a feature fails its written test, OPS rebuilds it free." Owner-approval recording mechanism corrected — the `spec_acceptance_events` row uses the new `owner_purchase_approved` event type, NOT `spec_communications`. Token-hash storage explicitly described in `/spec/checkout/[buyer_checkout_token]`. Conversion tracking expanded with `billing_address_submitted` (server) and `quebec_rejected` (internal-only) events.

**05_ADMIN_UX.md** — All `/admin/spec/*` routes are operator-only via `public.is_spec_operator()`. The customer-can-view language was removed from `/admin/spec/[id]`. Customer-facing project portal at `/account/spec/[id]` deferred to Phase 2 (gated on project membership, NOT on operator status). Added a page-header TEST MODE toggle (includes `is_test = true` rows when on) and a REFRESH BOARD button (POSTs to a gated server route that calls `refresh_spec_board_snapshot()`). The Refund queue now surfaces a per-milestone refund_breakdown preview that shows the processor's planned action (refund / void / credit_note / mark_uncollectible) before Process is clicked; the processed view renders the breakdown as a line-by-line summary table. Entitlements tab gained an enabled/disabled toggle with a reason picker tied to the constrained `disabled_reason` list (now including `'not_yet_delivered'`); the initial-reservation state is documented as `enabled = false`. Status-change side effects for `discovery → building` updated to reserve entitlements as `enabled = false`; `building → support` updated to flip them to `enabled = true`, `enabled_at = now()`. Permissions section rewritten to describe `is_spec_operator()` and to explicitly note why `has_permission(...)` is unsafe here.

**06_CONTRACT_AND_EMAILS.md** — §8 Delivery & 30-day guarantee rewritten to describe per-milestone refund mechanics (refund / void / credit_note / mark_uncollectible) and to acknowledge net-15 invoicing — the prior text claimed "Stripe Refunds API reverses P1-P4" which can't refund unpaid invoices. §22 Refund policy summary updated to match. §29 Boilerplate's Amendment clause gained a carve-out: amendments to material terms (pricing, scope, IP, refund mechanics, LIL) do not apply to engagements with a countersigned scope document prior to the amendment effective date without affirmative buyer acceptance. The `spec.retainer_active` email body was rewritten from "You're protected" (insurance-adjacent) to "Your retainer is active. SPEC work now uses the response window in your agreement." Notifications section gained a "company_id routing rules" subsection documenting that operator-facing rows use `OPS_OPERATIONS_COMPANY_ID`, customer-facing rows use `linked_company_id` (or `OPS_OPERATIONS_COMPANY_ID` for no-company-buyers until their company is provisioned).

**07_ROLLOUT.md** — Phase 1 migration list expanded from 5 files to 8 (added internal_company, operator_gate, snapshot_and_rls split from view_and_rls, storage). The auth+owner-approval gate section gained an explicit pre-Stripe billing-address step. The webhook handler section now describes populating `tos_version_accepted` / `tos_accepted_at` from null at deposit_paid. The admin section calls out `is_spec_operator()` (not the generic `has_permission`) and adds test-mode toggle + force-refresh-board button. Two new Phase 1 sub-sections added: §13 RLS audit follow-up (now a launch gate, not an open item) and §14 Test-mode column. Phase 2 list now leads with the customer-facing portal at `/account/spec/[id]`. Verification scenarios expanded from 15 to 18: Scenario 4 rewritten as pre-Stripe Quebec rejection (no `spec_projects` row, no Stripe call); Scenario 6 rewritten as guarantee invocation with mixed payment states (P1 paid, P2 paid, P3 open, P4 partial — exercises every refund action); Scenario 8 rewritten as the snapshot-table path with refreshed_at + Cache-Control; Scenario 15 rewritten as operator-gate denial of a customer-side company admin; new Scenario 16 (dual ToS acceptance for Path B); new Scenario 17 (token-hash storage); new Scenario 18 (operator-gated snapshot force-refresh). Manual checklist expanded from 13 to 17 items: RLS audit moved in as a launch gate, OPS Operations seed verification added, `is_spec_operator()` test against customer-company admin (must fail) and SPEC operator role (must pass) added, dual-acceptance-events verification added, refund flow tested across all four mechanics, `is_test` column verified. Open items reduced from 12 to 11: RLS audit and test-environment strategy moved to Phase 1; counsel review reframed as a recommended risk mitigation (not a launch blocker per locked answer Q7); a new note added that the `is_spec_operator()` body consults only `scope='all'` at launch — if `'global'` is added to the DB later, the function is updated in the same migration. Critical files list extended with `/spec/billing-address`, `/api/admin/spec/board/refresh`, `/account/spec/[id]` (Phase 2). `/admin/spec/layout.tsx` purpose updated to call out the `is_spec_operator()` gate over the generic `has_permission`.

### Cross-file consistency checks performed

- Every state transition in 03_WORKFLOW.md corresponds to a status in 02_DATA_MODEL.md's `spec_project_status` enum.
- Every status-change side effect in 05_ADMIN_UX.md matches the workflow in 03_WORKFLOW.md (including the new entitlement reservation/flip rules).
- The 12-month-fees LIL cap in 06_CONTRACT_AND_EMAILS.md § 14 matches the language in 01_BUSINESS_MODEL.md § 3 (both softened to "intended legal framing pending counsel review").
- The refund matrix in 01_BUSINESS_MODEL.md § 3 + the per-milestone refund-mechanics text are the single source of truth referenced from 03_WORKFLOW.md (processor pseudocode), 05_ADMIN_UX.md (queue UI + processed view), and 06_CONTRACT_AND_EMAILS.md §8 + §22.
- The walkthrough_completed_at anchor is consistent across 01_BUSINESS_MODEL.md (guarantee, support window, retainer offer, referral payout, multiplier first-bill), 03_WORKFLOW.md, and 06_CONTRACT_AND_EMAILS.md.
- The `hold_type` enum and slot-consumption logic are consistent across 01_BUSINESS_MODEL.md § 6, 02_DATA_MODEL.md, 03_WORKFLOW.md, 04_CUSTOMER_UX.md, and 05_ADMIN_UX.md.
- Queue count source-of-truth is locked: `awaiting_deposit` + `deposit_paid` everywhere (the snapshot function, the capacity-consuming-states section in 03, the next-start-date derivation in 02, and the admin Kanban in 05).
- RLS policies in 02_DATA_MODEL.md use `public.is_spec_operator()` consistently. The `admins` table is not used. `has_permission(...)` is not used.
- The `spec_public_board_snapshot.refreshed_at` field is referenced consistently in 02 (column), 04 (UI freshness signal), 05 (force-refresh result), and 07 (verification scenario).
- The `owner_purchase_approved` event_type is referenced consistently in 02 (CHECK constraint), 03 (Path B Step 6), 04 (owner-approval page write), 05 (Timeline tab), 07 (Scenario 16).
- The `refund_breakdown` jsonb is referenced consistently in 02 (column), 03 (processor pseudocode), 05 (UI preview + post-process view), 06 (§8 ToS text), 07 (Scenario 6).
- The `is_test` column is referenced consistently in 02 (per-table column), 03 (cap. snapshot excludes), 05 (admin toggle), 07 (verification + checklist).
- `OPS_OPERATIONS_COMPANY_ID` is referenced consistently in 02 (seed), 03 (webhook + intake notifications), 06 (notification routing), 07 (verification checklist).

### Locked answers applied (per Jackson, second-review follow-up session)

1. **Operator isolation (Q1):** `is_spec_operator()` function. Customer-side company admins do NOT satisfy the SPEC admin gate.
2. **Dual ToS acceptance (Q2):** Buyer accepts ToS at Stripe checkout. Account_holder accepts at owner-approval. Two `spec_acceptance_events` rows per Path B engagement.
3. **Province collection (Q3):** Collected in OPS pre-Stripe. Server-side Quebec rejection.
4. **Internal company (Q4):** OPS Operations seeded with a constant UUID. All operator-facing notifications + no-company-buyer notifications route through it.
5. **Board data path (Q5):** Snapshot table + pg_cron every 5 min + manual operator force-refresh + Vercel edge cache (Cache-Control max-age=300).
6. **Test mode (Q6):** Stripe test mode + `is_test boolean` column on every SPEC engagement table. Same Supabase project. Default false. Admin queries default-filter test rows.
7. **Counsel (Q7):** Self-review for now, counsel after first dispute. BC court certainty language softened. Open-item note retained for counsel-before-volume as recommended risk mitigation.

### Items NOT changed in this pass

- The three tier prices ($3K / $8.5K / $18K) and the 25/25/25/25 split.
- The CAD-only launch (USD considered later when measurable US demand exists).
- The $225/hr CAD change-order rate.
- The retainer monthly amounts ($250 / $450 / $750).
- The polish budget hours (2 / 4 / 8).
- The $500 referral bounty.
- The 30/60/90 day support windows.
- The BC governing law + BC Small Claims < $35k + BC Supreme > $35k + no mandatory arbitration.
- The OPS-owns-code, customer-licenses-non-transferable IP model.
- The no-automated-refunds rule.
- The general phased structure (Phase 0 mitigation, Phase 1 ads-ready, Phase 2 volume polish, Phase 3 scale).
- The full FAQ list + JSON-LD pattern + `<details>/<summary>` rendering rule on the marketing page.
- The 12-month-fees LIL cap (the framing was softened from BC-court-certainty language to "intended legal framing pending counsel review" — the cap itself is unchanged).

---

## 2026-05-25 — Second revision pass (post-external-review)

Read this before reviewing the full files. The original locked spec (2026-05-25 first pass) went through an external review that surfaced eight critical findings, eleven major findings, and seven minor findings, plus twelve locked decisions from a follow-up session with Jackson. This pass rewrites all eight SPEC files to incorporate every one.

### Per-file summary

**README.md** — Updated navigation to reflect new files and the four-phase rollout. Added a "what changed in this pass" pointer to CHANGELOG.md. Documented the verified identity model (`get_user_id` returns text, `has_permission` takes uuid, the `spec.admin` permission as the canonical SPEC admin gate). Added Quebec exclusion and multi-engagement support to the front-matter.

**01_BUSINESS_MODEL.md** — Largest single rewrite. The owner-approval-before-Stripe gate was inverted (Stripe was charging first and asking the owner second; now no Stripe charge happens until the account_holder approves). The refund matrix was made into a single locked table used consistently. The Limitation of Liability was reframed from "fees paid after the 30-day window" (which collapsed to $0 for customers without retainers) to "fees paid in the 12 months prior to the claim, less any refunds already issued, with standard carve-outs and consequentials always excluded." Quebec is excluded; regulated workflows (PHI/PHIPA, PCI raw card, regulated credit, surveillance, CASL-violating bulk messaging) are explicit prohibitions. Multi-engagement support is documented and drives the entitlement model change. Annual subscriber proration is specified. The 30-day guarantee gains explicit anti-abuse rules (one invocation per engagement; no claim after chargeback or material breach or during non-payment disabled period; customer must cease module use upon refund). Referral payouts now require Stripe Connect KYC, T4A reporting at $500+/yr, self-referral + related-entity detection, payout holds for disputed projects. The hold model is split into `customer_requested` (frees slot) and `ops_blocked` (consumes slot) to resolve the original spec's contradiction. The support window, the guarantee window, the referral payout eligibility, and the subscription multiplier first-bill date all anchor on `walkthrough_completed_at`. The "Jackson is sole judge" phrasing was softened to "OPS determines estimate and classification using the signed scope and acceptance criteria."

**02_DATA_MODEL.md** — Schema rewritten against the live identity model verified on 2026-05-25 via Supabase MCP. Three new tables added: `spec_scope_documents` (versioned SOW with content_hash, content_json, supersedes), `spec_acceptance_events` (legal evidence chain — ToS accept, scope signoff, midpoint accept, delivery accept, change order accept — with IP, user agent, signature_method, payload_hash), and `spec_module_entitlements` (per-engagement, per-module enable/disable + multiplier + surcharge — replaces the two booleans the original spec proposed on `companies`). A fourth new table — `spec_owner_approval_requests` — supports the new approval-before-Stripe flow with one-time approval and buyer_checkout tokens. The `companies` table extensions are dropped entirely; entitlements live in the new table. The `spec_blocked_buyers` table got an explicit FK and a check on `blocked_by_user_id`. FK columns across the schema (buyer_user_id, account_holder_user_id, linked_company_id, verified_by_user_id, processed_by_user_id, blocked_by_user_id, accepted_by_user_id, logged_by_user_id) now have real `references` clauses with deliberate on-delete behaviour (set null for soft refs, cascade for owned children, restrict for required-author refs). The `hold_type` enum is added; `prior_status`, `resume_requested_at` fields added to `spec_projects`. The retainer-tickets concept is merged into `spec_support_tickets` via a `phase` enum (support/retainer/ad_hoc). The `spec_board_counts` view is reworked: now `security_invoker = on`, exposes only coarse signals (`availability` bucket, `waitlist` range, `next_start_week`) — no exact counts, no `last_activity_at`. RLS policies rewritten to use `public.has_permission(public.get_user_id()::uuid, 'spec.admin', 'all')` instead of the original (non-functional) `auth.uid() = buyer_user_id` and `admins.user_id` references. The `spec.admin` permission is seeded into `role_permissions` for Jackson's admin role. Migration filenames split into five smaller files for clarity. The `spec_refund_requests` table gets a unique partial index enforcing one guarantee invocation per project. `spec_referrals` gets new statuses (`kyc_required`, `review`, `held`) and flags (`self_referral_flag`, `related_entity_flag`, `ytd_referrer_payout_cents`, `t4a_required`). Attribution columns added to `spec_projects` (`attribution` JSONB carrying UTM + gclid + fbclid + landing_url + first_touch_at).

**03_WORKFLOW.md** — Customer journey rewritten to lead with the Path A (buyer = account_holder) vs Path B (buyer ≠ account_holder) branching. Path B explicitly does NOT create a Stripe Checkout Session until the account_holder approves; a short-lived `buyer_checkout_token` is then issued to the buyer to complete payment. Added `awaiting_owner_approval` → `awaiting_deposit` → `deposit_paid` to the state machine. Capacity-consuming-state logic updated for the `hold_type` split. The state diagram redrawn. Sideways-branches section gains an owner-decline path, an approval-token-expiry path, and a clarified ops_blocked-hold path. The webhook flow now records `spec_acceptance_events` for `tos_accepted` (and other events as they occur). Server-side conversion events fire at every funnel step. Walkthrough_completed_at is stamped as the canonical anchor for everything downstream (support, guarantee, referral, retainer offer, multiplier first-bill).

**04_CUSTOMER_UX.md** — OPS BOARD reworked to read the sanitized public view — coarse signals only (`OPEN`/`LIMITED`/`WAITLIST`), waitlist range (`0`/`1-2`/`3+`), week-of next-start. The original "ACTIVE 2/4, QUEUE 1" exposure was a leak for low-volume tiers. The "Pay as we deliver" copy was rewritten to "Pay as work clears review" — the original "you pay for work you've seen" claim was false for P2 (which fires at scope sign-off, before build work). The unverified "500+ businesses, 12K projects, 85K tasks, $14M revenue" stat block was removed entirely from both English and Spanish dictionaries with placeholder for real testimonials when available. FAQ component must use `<details>/<summary>` or pre-rendered-visible pattern so answers ship in initial HTML (the client-side accordion breaks SEO + AI discovery). Added `/spec/awaiting-approval` (Path B wait state) and `/spec/checkout/[buyer_checkout_token]` (post-approval checkout launcher). Added the Quebec address rejection + regulated-workflow attestation flow to the intake form. Added a full Conversion Tracking section specifying client + server events, UTM + GCLID + FBCLID first-touch persistence (30 days), Meta CAPI + Google Enhanced server-side sends with outbox + retry for failures. Numbers compliance audit explicitly called out (JetBrains Mono tabular-lining slashed-zero everywhere). Spanish dictionary update explicitly noted as Phase 1 deliverable.

**05_ADMIN_UX.md** — Default view restructured to put a TODAY command queue at the top (money to collect, customers blocked on approval, decisions due, SLA misses, refund/dispute risk, next best action), Kanban moved below. Pipeline gains `awaiting_owner_approval` and `awaiting_deposit` columns and visually splits `on_hold` into `customer_requested` (top) and `ops_blocked` (bottom). New `/admin/spec/owner-approvals` queue page. Project detail page gains an Entitlements tab driven by `spec_module_entitlements`. Scope doc tab now exposes the `spec_scope_documents` version history. Timeline tab surfaces `spec_acceptance_events` rows. Refund queue gains eligibility chips (within 30 days? no chargeback? no breach? no non-payment?) computed before Process is enabled. Permissions language replaced — `has_permission('spec.admin')` is the canonical gate; the legacy `admins` table is explicitly NOT used.

**06_CONTRACT_AND_EMAILS.md** — ToS rewritten from 17 to 29 sections. Added: Eligibility + geographic scope (Quebec exclusion), Acceptable use + prohibited use (regulated workflows refusal language verbatim), Privacy + data processing (cross-references to existing /legal Privacy + DPA tabs), Subprocessors (Stripe, Supabase, SendGrid, Vercel — listed), Security practices (high level), Warranty disclaimer, Off-boarding + data retention (90-day default), Non-solicitation (mild — no poaching OPS staff), Audit limits (no customer-driven audits without paid engagement), Feedback (OPS may use freely), Assignment (OPS may assign on acquisition), Order of precedence (ToS > scope doc > other written communications), ToS amendment notice rights. Limitation of Liability section rewritten to the 12-month-fees cap with carve-outs. Email subject lines made tactical and UPPERCASE per OPS voice (`SPEC INTAKE WAITING`, `SPEC PAUSED`, `REFUND PROCESSED`). New templates added for the owner-approval flow (`spec.owner_approval_required`, `_granted`, `_declined`) and for the referral + ToS-update flows. Cron jobs section adds owner-approval expiry check, ops_blocked-hold check, conversion-tracking outbox flush. Notification rail entries expanded to cover every new event.

**07_ROLLOUT.md** — A new Phase 0 added at the top: immediate safety mitigation for the live `/spec` page (two options — replace deposit CTAs with conversation-only, or feature-flag gate them off — to be deployed today, ahead of Phase 1). Phase 1 expanded to include: ToS + Privacy + DPA updates as a single lawyer-review pass, owner-approval flow files, sanitized OPS BOARD view, conversion tracking infra (Meta CAPI + Google Enhanced + first-touch cookies + outbox), TODAY command queue admin UI, Entitlements tab on project detail, regulated-workflow attestation in intake, Quebec block, and `spec.admin` permission seed. Verification scenarios expanded from 10 to 15, adding: Quebec rejection, regulated workflow attestation flagged, 30-day guarantee invocation with anti-abuse, dispute closes guarantee window, ops_blocked hold keeps slot consumed while customer_requested frees it, owner-approval expiry, permission check denies non-admin. Critical files list extended to include all new components and routes. Open items list updated with Phase 0 mitigation, lawyer-review scope (now three documents), Meta CAPI + Google Enhanced credential procurement, Stripe Connect Express setup for Phase 2 referrals, and T4A issuance workflow.

### Cross-file consistency checks performed

- Every state transition in `03_WORKFLOW.md` corresponds to a status in `02_DATA_MODEL.md`'s `spec_project_status` enum.
- Every status-change side effect in `05_ADMIN_UX.md` matches the workflow in `03_WORKFLOW.md`.
- The 12-month-fees LIL cap in `06_CONTRACT_AND_EMAILS.md` § 14 matches the language in `01_BUSINESS_MODEL.md` § 3.
- The refund matrix in `01_BUSINESS_MODEL.md` § 3 is the single source of truth referenced from `03_WORKFLOW.md`, `05_ADMIN_UX.md`, and `06_CONTRACT_AND_EMAILS.md` § 22.
- The walkthrough_completed_at anchor is consistent across `01_BUSINESS_MODEL.md` (guarantee, support window, retainer offer, referral payout, multiplier first-bill), `03_WORKFLOW.md`, and `06_CONTRACT_AND_EMAILS.md`.
- The `hold_type` enum and slot-consumption logic are consistent across `01_BUSINESS_MODEL.md` § 6, `02_DATA_MODEL.md`, `03_WORKFLOW.md`, and `05_ADMIN_UX.md`.
- RLS policies in `02_DATA_MODEL.md` use the verified live identity model (`public.has_permission(public.get_user_id()::uuid, 'spec.admin', 'all')`); the `admins` table is not used.
- Conversion-tracking event names are consistent between `04_CUSTOMER_UX.md` and the file list in `07_ROLLOUT.md`.

### Items NOT changed in this pass

- The three tier prices ($3K / $8.5K / $18K) and the 25/25/25/25 split.
- The CAD-only launch (USD considered later when measurable US demand exists).
- The $225/hr CAD change-order rate.
- The retainer monthly amounts ($250 / $450 / $750).
- The polish budget hours (2 / 4 / 8).
- The $500 referral bounty.
- The 30/60/90 day support windows.
- The BC governing law + BC Small Claims < $35k + BC Supreme > $35k + no mandatory arbitration.
- The OPS-owns-code, customer-licenses-non-transferable IP model.
- The no-automated-refunds rule.
- The general phased structure (Phase 1 = ads-ready, Phase 2 = volume polish, Phase 3 = scale).
